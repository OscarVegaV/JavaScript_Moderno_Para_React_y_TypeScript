// Variable


//let has 2 characteristics Allows reassignment: you can change the value of the variable after it’s declared. Block scope: the variable exists only within the {} block where it was declared.
let customer = "Oscar";
customer = 33;
console.log(customer);// ouput 33

let price;
price = 1000
console.log(price); // ouput 1000